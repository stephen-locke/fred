services:
  - type: web
    name: fred-dashboard
    env: python
    buildCommand: pip install -r requirements.txt
    startCommand: gunicorn fred_dashboard:server
    envVars:
      - key: FRED_API_KEY
        sync: false
